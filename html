<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Pour mon amoureuse pour la vie</title>
  <style>
    body {
      background: linear-gradient(135deg, #ffdde1, #ee9ca7);
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: "Comic Sans MS", cursive;
      overflow: hidden;
    }

    .card {
      background: white;
      padding: 40px;
      border-radius: 20px;
      text-align: center;
      box-shadow: 0 10px 30px rgba(0,0,0,0.2);
      position: relative;
    }

    h1 {
      color: #ff4d6d;
      margin-bottom: 30px;
    }

    button {
      font-size: 20px;
      padding: 10px 25px;
      margin: 10px;
      border: none;
      border-radius: 30px;
      cursor: pointer;
      transition: transform 0.2s;
    }

    button:hover {
      transform: scale(1.1);
    }

    .yes {
      background: #ff4d6d;
      color: white;
    }

    .no {
      background: #ccc;
    }

    .message {
      margin-top: 20px;
      font-size: 24px;
    }

    .heart {
      position: absolute;
      font-size: 24px;
      animation: float 3s linear infinite;
    }

    @keyframes float {
      from {
        transform: translateY(0);
        opacity: 1;
      }
      to {
        transform: translateY(-150px);
        opacity: 0;
      }
    }
  </style>
</head>
<body>

  <div class="card">
    <h1>Est ce que tu veux être my Valentine ? </h1>

    <button class="oui" onclick="yesClick()">OUIIIIIIIIIII</button>
    <button class="non" onclick="noClick()">NON...</button>

    <div class="message" id="message"></div>
  </div>

  <script>
    function yesClick() {
      const message = document.getElementById("message");
      message.innerHTML = "BHA OUI ENFT";

      for (let i = 0; i < 15; i++) {
        createHeart();
      }
    }

    function noClick() {
      const message = document.getElementById("message");
      message.innerHTML = "😔💔";
    }

    function createHeart() {
      const heart = document.createElement("div");
      heart.classList.add("heart");
      heart.innerHTML = "💖";
      heart.style.left = Math.random() * window.innerWidth + "px";
      heart.style.bottom = "0px";
      document.body.appendChild(heart);

      setTimeout(() => {
        heart.remove();
      }, 3000);
    }
  </script>

</body>
</html>
